{{ template "layout" . }}

{{ define "title" }}Home — Proto{{ end }}

{{ define "content" }}
<section>
  <p>部落格原型（Gin + SSR + SQLC）。</p>
  <p>Docs: <a href="{{ .DocsURL }}" target="_blank" rel="noreferrer noopener">Gin Docs</a></p>
  <p>Health: <code>/healthz</code></p>
  <p>API: <code>/api/articles?limit=10&offset=0</code></p>
</section>
{{ end }}


