# Adapters (blog/post)

This context exposes public HTTP surfaces for posts:

- `adapters/api`: JSON API handlers (public).
- `adapters/public`: SSR/SEO handlers (public HTML).
- `adapters/view`: presenters for JSON/HTML payloads.
